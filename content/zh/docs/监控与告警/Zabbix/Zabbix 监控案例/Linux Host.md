---
title: "Linux Host"
---



# Linux Host 核心监控项

- 内存、CPU、磁盘、网络、IO等
- 通常选择zabbix自带的模板，如：Template OS Linux by Zabbix agent active







# 先决条件

- 被监控主机安装zabbix-agent





# 实现

## 环境

| IP地址    | 服务                                | 端口        |
| --------- | ----------------------------------- | ----------- |
| 10.0.0.8  | zabbix-server、msqyl(zabbix-server) | 10051、3306 |
| 10.0.0.28 | Java-gateway、Linuxhost             | 10052       |

## 部署环境

- 过程省略...

## web界面配置

### 配置

#### 主机

- 右上角创建主机

- **主机名称：**被监控主机中zabbix-agent所定义的Hostname，Hostname一般是被监控主机的IP，所以这里直接写IP即可
  - 如：10.0.0.28
- **可见的名称：**输入一个对外展示的，可以使用字母数字、空格、点”."、中划线"-"、下划线"_"，如果不写此项则会使用前面定义的主机名称
  - 如：10.0.0.28-linux-host
- **群组：**选择一个或多个已经定义的分组，群组如果事先没有创建的话会根据定义的群组名称自动创建，所有访问权限都分配到主机组，而不是单独的主机。这也是主机需要属于至少一个组的原因。
  - 如：web
- **Interfaces：**定义被监控主机的IP地址等信息，端口号一般默认10050即可
  - 如：10.0.0.18
- **由agent代理程序监测：**涉及到proxy代理节点时需要配置

#### 模板

- 定义完上面的主机后 需要选择监控的模板，直接搜索然后选择即可 如：

- **此模板仅作测试 生产中需要自定义模板**

- ```
  Template OS Linux by Zabbix agent active
  ```

#### 后记

- 按照以上的方式配置完成后，可以在主机栏中点击图形-->选择监控项名称-->预览来观察是否有数据出现，也可以点击监测-->主机来查看是否有数据采集信息出现

