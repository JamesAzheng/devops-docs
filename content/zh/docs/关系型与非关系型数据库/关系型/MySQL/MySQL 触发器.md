# TRIGGER 触发器

- 触发器的执行不是由程序调用，也不是由手工启动，而是由事件来触发、激活从而实现执行
- 通常根据增删改的操作来触发执行
- 还分为事前触发和事后触发
- 触发器相关文件（存放在数据库对应的目录下以）
  -  ***.TRN** 触发器相关文件
  -  ***.TRG** 被监控表的触发器相关文件

- 触发器范例
  - 如：在电商网站购物，将商品生成订单就是执行了一条SQL语句，而触发器发现这条SQL语句执行后 则会将商品的总库存减去下单的数量
  - **生产中触发器基本不用**


